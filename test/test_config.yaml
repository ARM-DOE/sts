directory: watch_directory
sender_threads: 10
log_file_duration_hours: 3000
cache_file_name: file_cache.dat
disk_path: external_disk
dynamic:
  compression: false
  bin_size: 3000000
  tags:
    DEFAULT:
      priority: 5
      transfer_method: HTTP
    radar:
      priority: 3
      transfer_method: Disk
    laser:
      priority: 2
      transfer_method: HTTP
    log:
      priority: 1
      transfer_method: HTTP